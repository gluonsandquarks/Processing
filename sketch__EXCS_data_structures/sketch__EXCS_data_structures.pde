
// indexOf() -> Searches for an index that matches the string in the input.
// EX: someString.indexOf("stringToSearchForInSomeString")
// Returns a -1 when it doesn't find a match. -> Pretty useful.


// substring() returns a section of a string. Accepts a start and end index.
// EX: someString.substring(0,5); -> will return the first 5 characters of the string.
// This is because this method will give back the character in the first index all the way until
// the character of the last index minus one.

void setup(){

}

void draw(){

}
